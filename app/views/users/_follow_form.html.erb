<% if current_user %>
  <div id="follow_form">
    <% if current_user.following? user %>
      <%= render "unfollow", del_relation: del_relation %>
    <% else %>
      <%= render "follow", new_relation: new_relation %>
    <% end %>
  </div>
<% end %>
